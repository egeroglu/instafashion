export interface User {
    id: string;
    email: string;
    username: string;
  }
  
  export interface Product {
    id: string;
    name: string;
    image: string;
    price: number;
    description: string;
  }